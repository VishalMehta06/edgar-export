# Copy this file to .env and fill in your values.
# .env should be added to .gitignore — never commit it.

# Flask session secret key — generate a strong random string for production.
# Example: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-me-with-a-strong-random-secret

# Logging verbosity.
# WARNING (default) — only warnings and errors are written to app.log (less I/O).
# DEBUG             — full debug output including every request and parsed result.
LOG_MODE=WARNING
