# EDGAR-EXPORT

To run the app:
```shell
python3 routes.py
```

*NOTE: This is only meant to be ran locally, NOT HOSTED.*

The authentication (if you can call it that) **IS NOT SECURE** and is only used to fill the user agent field of SEC EDGAR requests. 